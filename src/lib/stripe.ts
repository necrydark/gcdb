// const 

// export const hasBasicAccess =
//     userSubscription?.subscriptionStatus === "active" &&
//     userSubscription?.stripePriceId === process.env.NEXT_PUBLIC_BASIC_PRICE_ID;
// export  const hasPremiumAccess =
//     userSubscription?.subscriptionStatus === "active" &&
//     userSubscription?.stripePriceId === process.env.NEXT_PUBLIC_PREMIUM_PRICE_ID;